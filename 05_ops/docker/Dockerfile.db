# KithLy Database - PostgreSQL with PostGIS
FROM postgis/postgis:16-3.4

# Copy schema files
COPY 01_bedrock/schema/*.sql /docker-entrypoint-initdb.d/

ENV POSTGRES_DB=kithly
ENV POSTGRES_USER=kithly_app
